name: CD - Deploy to Databricks

on:
  workflow_run:
    workflows: ["CI - Build Scala Project"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Debug artifact info
        run: |
          echo "CI Workflow run ID: ${{ github.event.workflow_run.id }}"
          echo "Attempting to download artifact 'eurowings-scala-assembly'"

      - name: Download artifact from CI
        uses: dawidd6/action-download-artifact@v6
        with:
          workflow: ci.yml
          name: eurowings-scala-assembly
          path: target/

      - name: List downloaded files
        run: |
          echo "Listing target directory contents..."
          ls -R target || echo "⚠️ No target folder found!"

      - name: Install Databricks CLI (v0.275.0)
        run: |
          DATABRICKS_VERSION="0.275.0"
          echo "Installing Databricks CLI v$DATABRICKS_VERSION..."
          curl -L -o databricks_cli.zip \
            "https://github.com/databricks/cli/releases/download/v${DATABRICKS_VERSION}/databricks_cli_${DATABRICKS_VERSION}_linux_amd64.zip"
          
          mkdir -p ~/databricks
          unzip -o databricks_cli.zip -d ~/databricks
          chmod +x ~/databricks/databricks
          
          export PATH="$HOME/databricks:$PATH"
          echo "$HOME/databricks" >> $GITHUB_PATH
          
          echo "Verifying installation..."
          ~/databricks/databricks version || echo "⚠️ Databricks CLI check failed"

      - name: Set Databricks credentials
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          echo "✅ Databricks credentials set (via env vars)."

      - name: Verify Databricks Authentication
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          echo "Testing Databricks token with new CLI..."
          ~/databricks/databricks workspace list / || (echo "❌ Invalid Databricks token or insufficient scope" && exit 1)
          echo "✅ Token is valid and workspace is accessible."

      - name: Upload JAR to Databricks DBFS (Private Root)
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          echo "Uploading JAR to Databricks private DBFS path..."
          JAR_FILE=$(find target -name "*.jar" | head -n 1)
          if [ -z "$JAR_FILE" ]; then
            echo "❌ No JAR file found in target directory!"
            exit 1
          fi
          echo "Found JAR: $JAR_FILE"
          PRIVATE_PATH="dbfs:/mnt/ci-artifacts/eurowings-scala-assembly-latest.jar"
          ~/databricks/databricks fs cp "$JAR_FILE" "$PRIVATE_PATH" --overwrite || (echo "❌ Upload failed, check DBFS mount permissions" && exit 1)
          echo "✅ Upload completed to $PRIVATE_PATH."

      - name: Trigger Databricks Job
        env:
          DATABRICKS_HOST: ${{ secrets.DATABRICKS_HOST }}
          DATABRICKS_TOKEN: ${{ secrets.DATABRICKS_TOKEN }}
        run: |
          echo "🚀 Triggering Databricks job..."
          ~/databricks/databricks jobs run-now --job-id ${{ secrets.DATABRICKS_JOB_ID }}
