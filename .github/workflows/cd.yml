name: CD - Deploy to Databricks

on:
  workflow_run:
    workflows: ["CI - Build Scala Project"]
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: ${{ github.event.workflow_run.conclusion == 'success' }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # 👇 Debug step added here (before downloading artifact)
      - name: Debug artifact list
        run: echo "Attempting to download artifact from CI run..."

      - name: Download artifact from CI
        uses: actions/download-artifact@v4
        with:
          name: eurowings-scala-assembly
          path: target/

      - name: Set up Databricks CLI
        run: |
          curl -fsSL https://github.com/databricks/cli/releases/latest/download/databricks_linux_amd64.zip -o databricks.zip
          unzip databricks.zip -d ~/databricks
          echo "$HOME/databricks" >> $GITHUB_PATH
          databricks version

      - name: Configure Databricks Auth
        run: |
          databricks configure --host "${{ secrets.DATABRICKS_HOST }}" --token "${{ secrets.DATABRICKS_TOKEN }}"

      - name: Upload JAR to Databricks DBFS
        run: |
          databricks fs cp "target/scala-2.12/eurowings-scala-assembly-0.1.0.jar" dbfs:/FileStore/jars/eurowings-scala-assembly-0.1.0.jar --overwrite

      - name: Trigger Databricks Job
        run: |
          databricks jobs run-now --job-id ${{ secrets.DATABRICKS_JOB_ID }}