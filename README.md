# Eurowings Scala Challenge (Task 1)
- Ingest `Dataset_YYYYMMDD.csv` → Bronze (raw) → Silver (typed + MoM `Erlöswert`).
- VS Code friendly. Configure paths in `conf/application.conf`.
- Run: `sbt run` or `sbt "run --dataset Vertriebskanal --ingestionDate 2025-10-01"`.
